/** Default session budget: undefined means no session limit. */
export const DEFAULT_SESSION_BUDGET = undefined;

/** Default daily budget: undefined means no daily limit. */
export const DEFAULT_DAILY_BUDGET = undefined;

/** Token count per pricing unit (1K tokens). */
export const TOKENS_PER_1K = 1000;

/** Daily budget reset interval: 24 hours in milliseconds. */
export const DAILY_RESET_INTERVAL_MS = 86_400_000;
